# 🚀 Blockchain Enterprise 进阶优化路线图 (Roadmap)

本项目已完成核心的企业级架构重构、RBAC 权限系统、物理冷钱包签名、以及 Gunicorn + WAL 并发性能优化。以下为后续计划实施的进阶优化点，旨在进一步提升系统的安全性、合规性和运维便捷性。

### 1. 🔐 安全增强：双因素认证 (TOTP 2FA)
*   **目标**：为 `SUPER_ADMIN` 和 `MANAGER` 角色引入强制 2FA 校验。
*   **方案**：集成 `PyOTP` 库，支持 Google Authenticator。
*   **收益**：防止管理员账号因密码泄露导致的资产篡改风险。

### 2. 🛡️ 存储增强：SQLCipher 数据库加密
*   **目标**：实现数据库文件的“静态加密 (Encryption at Rest)”。
*   **方案**：将标准 SQLite 替换为 `pysqlcipher3`。
*   **收益**：确保即使 `.db` 文件被物理窃取，内容也无法被读取。

### 3. 🤝 协作增强：资产多签机制 (Multi-Sig)
*   **目标**：对高价值资产实现“联合记账”。
*   **方案**：定义多签规则，关键区块需两枚不同的物理私钥签名方可生效。
*   **收益**：从制度和技术双重层面杜绝内部单人作案。

### 4. 🐳 运维增强：Docker 容器化部署
*   **目标**：实现跨平台的“一键部署”。
*   **方案**：编写 `Dockerfile` 和 `docker-compose.yml`，封装 Python 环境及 `libzbar` 底层库。
*   **收益**：彻底解决不同操作系统（Linux/Windows）带来的环境差异和底层库依赖问题。

### 5. ⏰ 自动化审计：定时全链巡检汇报
*   **目标**：由“被动审计”转向“主动防御”。
*   **方案**：利用 `apscheduler` 开启后台任务，定期自动执行全量校验。
*   **收益**：系统自动发现并上报任何潜在的数据篡改或逻辑异常。

---
### ✅ 已完成里程碑 (Completed)
*   [x] **V0.4 核心逻辑重构**：完成函数向类的演进（`EnterpriseLedger`）。
*   [x] **RBAC 权限系统**：内置四种企业角色及 Session 鉴权。
*   [x] **用户管理后台**：支持管理员在线增删账号。
*   [x] **物理冷钱包模式**：支持物理 U 盘私钥动态插拔加载。
*   [x] **生产级性能优化**：完成 Gunicorn 适配与 SQLite WAL 模式切换。
*   [x] **工业级审计报告**：实现带 Report ID 的 HTML 报告导出。

---
*最新更新时间: 2026-04-23*
